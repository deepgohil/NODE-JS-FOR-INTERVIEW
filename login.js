const nameprint=function (name,age) {
    console.log(`my nanme is ${name} and age is ${age}`);
}
const genderp=function (gender) {
    console.log(`Your gender is ${gender}`);
}

module.exports={
    nameprint,
    genderp
}